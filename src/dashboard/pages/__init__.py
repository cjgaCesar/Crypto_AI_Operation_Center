"""
Páginas del Dashboard (Etapa 5).

Cada módulo expone una función render(service: DashboardService) que
dibuja esa página. app.py decide cuál llamar según la navegación lateral;
no se usa la convención automática de nombres numerados de Streamlit
(1_..., 2_...) en esta iteración, a propósito, para mantener la
navegación centralizada en app.py.
"""
